

<div class="container">
  <% if user_signed_in? %>
    <div class="row">
      <div class="col-xs-6">
        <h1><%= link_to "My Store", shop_path %></h1>
      </div>
      <div class="col-xs-6 text-right">
        <h1 class="cart-text"><%= render 'layouts/cart_text' %></h1>
      </div>
    </div>
  <% else %>
  <p>Not connected</p>
  <% end %>
<hr>
</div>
